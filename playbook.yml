#!/usr/bin/env ansible-playbook

---
- name: play 'playbook.yml'
  hosts: all
  become: yes

  vars_files:
    - defaults/main.yml

  roles:
    - { role: rvm_io.ruby,
        tags: ruby,
        rvm1_rubies: ['ruby-2.3.1'],
        rvm1_user: 'vagrant'
      }
    - { role: geerlingguy.mysql,
        tags: mysql
      }

# vim:ft=ansible:
